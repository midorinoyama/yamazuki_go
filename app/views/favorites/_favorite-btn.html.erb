<% if post.favorited_by?(current_user) %>
  <%= link_to post.favorites.count, post_favorites_path(post), method: :delete, class: "fas fa-heart heart_red" %>
<% else %>
  <%= link_to post.favorites.count, post_favorites_path(post), method: :post, class: "fas fa-heart heart_blue" %>
<% end %>