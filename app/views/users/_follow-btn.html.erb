<% if current_user.following?(user) %><%#フォローしているかどうか判断%>
   <%= link_to "フォロー中", user_relationships_path(user.id), method: :delete, class: "btn btn-light btn-sm" %>
<% else %>
   <%= link_to "フォロー", user_relationships_path(user.id), method: :post, class: "btn btn-primary btn-sm" %>
<% end %>
